## 一、白屏问题（网速慢）

### 1. 骨架屏（Skeleton Screen） ⭐ 最主流方案

- 在数据返回前，展示与页面布局一致的灰色占位块（模拟内容轮廓）
- 实现方式：
  - 手动写骨架组件（精确但维护成本高）
  - 自动生成：如 `vue-skeleton-webpack-plugin`、`page-skeleton-webpack-plugin`
  - CSS 动画：灰色块 + shimmer 闪光动画效果

### 2. Loading 动画 / 进度条

- 顶部进度条：如 NProgress，在路由切换时自动显示
- 全屏 Loading 遮罩：请求数据期间展示

### 3. SSR（服务端渲染）/ SSG（静态生成）

- 使用 Nuxt.js（Vue 生态）在服务端直接返回 HTML
- 用户拿到的不是空的 `<div id="app"></div>`，而是已经有内容的 HTML
- 从根本上解决白屏，因为浏览器不需要等 JS 下载执行完才渲染

### 4. 首屏优化（减少白屏时间）

- 路由懒加载：

  ```js
  () => import("./views/Home.vue");
  ```

- 代码分割：Vite/Webpack 自动 Code Splitting
- CDN 加速：静态资源放 CDN
- Gzip/Brotli 压缩：减小传输体积
- 预加载：`<link rel="preload">` 关键资源

## 二、加载失败（网络错误、接口报错、超时）

### 1. 错误边界组件（Error Boundary）

- 正常流程：Loading → 成功 → 展示内容
- 异常流程：Loading → 失败 → 展示错误页（带重试按钮）
- Vue 中可以用 `onErrorCaptured` 钩子捕获子组件错误

### 2. 请求层统一处理

- Axios 拦截器：在响应拦截器中统一处理 401、403、500 等状态码
- 超时重试：设置合理的 timeout，失败后自动重试 2-3 次
- 断网检测：监听 `navigator.onLine` 和 `window.addEventListener('offline')`，提示用户检查网络

### 3. 兜底 / 降级策略

- 缓存优先：用 Service Worker 缓存上次成功的数据，离线也能看
- 默认数据：接口挂了就展示本地默认数据或缓存
- 降级页面：展示简化版页面而不是完全空白

### 4. 图片加载失败

- `<img>` 标签的 `@error` 事件，失败时替换为默认占位图
- 企业中通常封装一个 `<BaseImage>` 组件，自带 loading + error 状态

## 三、后端资源返回及时（正常渲染）

### 核心流程：

请求发出 → 数据返回 → 响应式数据更新 → Vue 自动重新渲染 DOM

### 企业级的标准做法：

#### 1. 三态管理模式（几乎所有企业项目的标配）

```js
const state = reactive({
  loading: true, // 加载中 → 显示骨架屏/Loading
  error: null, // 加载失败 → 显示错误页
  data: null, // 加载成功 → 显示正常内容
});
```

模板中：

```vue
<Skeleton v-if="state.loading" />
<ErrorPage v-else-if="state.error" :error="state.error" @retry="fetchData" />
<Content v-else :data="state.data" />
```

#### 2. 组合式函数封装（Composable）

企业项目会封装通用的 `useFetch` / `useRequest`：

```js
const { data, loading, error, retry } = useRequest("/api/user");
```

一行代码搞定三态，所有页面复用。类似的库有 VueUse 的 useFetch、VueRequest 等。

## 总结：企业级状态流转图

```
页面加载
│
├─ 立即展示 → 骨架屏 / Loading 动画（解决白屏）
│
├─ 数据请求中...
│ │
│ ├─ 成功 → 隐藏骨架屏 → 渲染真实内容
│ │
│ ├─ 失败 → 展示错误页 + 重试按钮
│ │ └─ 重试 → 回到"数据请求中"
│ │
│ └─ 超时 → 自动重试 2-3 次 → 仍失败 → 错误页
│
└─ （可选）SSR 直接返回完整 HTML，跳过白屏阶段
你现在代码里的 v-if / v-else-if / v-else 三态切换，思路已经是对的了！企业级就是在这个基础上：骨架屏替代文字Loading + 封装通用请求逻辑 + 统一错误处理。
```
